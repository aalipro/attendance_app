{
  "name": "suivi-presences-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n BACK,FRONT -c green,cyan \"npm:dev --workspace apps/backend\" \"npm:dev --workspace apps/frontend\"",
    "dev:back": "npm run dev --workspace apps/backend",
    "db:reset": "npm run db:reset --workspace apps/backend",
    "build": "npm run build --workspace apps/backend && npm run build --workspace apps/frontend",
    "start": "npm run start --workspace apps/backend",
    "test": "npm run test --workspace apps/backend && npm run test --workspace apps/frontend || exit 0",
    "lint": "npm run lint --workspace apps/backend && npm run lint --workspace apps/frontend",
    "format": "npm run format --workspace apps/backend && npm run format --workspace apps/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
